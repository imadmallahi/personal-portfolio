import { EN_TRANSLATIONS } from './en';
import { FR_TRANSLATIONS } from './fr';

export type TranslationKeys = typeof EN_TRANSLATIONS;

export const TRANSLATIONS = {
    en: EN_TRANSLATIONS,
    fr: FR_TRANSLATIONS
};
